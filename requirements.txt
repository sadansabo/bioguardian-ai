# Core Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Core ML & Data
torch>=2.0.1,<3.0.0
torchaudio>=2.0.2,<3.0.0
librosa==0.10.1
numpy==1.24.3
scikit-learn==1.3.2
pandas==2.0.3

# Hugging Face Ecosystem (useful for datasets & transformers later)
huggingface_hub>=0.20.0

# Audio processing
soundfile==0.12.1 # Often needed for torchaudio on Linux

# Pydantic is now a dependency of FastAPI, but we can specify a compatible version
pydantic==2.5.0
